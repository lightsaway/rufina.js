var express = require('express');
var router = express.Router();

/* GET history data. */
router.get('/history', function(req, res, next) {

});

/* GET history for some instance. */
router.get('/history/:id', function(req, res, next) {

});

module.exports = router;
